# only search with sensitivity if there is mixed case
--smart-case
# follow symlinks
--follow

# add some types for specific searches
--type-set=css=.css,.less,.stylus
--type-set=coffeescript=.coffee

# ignore node_module directories
--ignore-dir=node_modules
